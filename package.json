{
  "name": "group-reduce",
  "version": "0.2.0",
  "description": "Group an array by a (computed) property and reduce it",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:mickvangelderen/group-reduce"
  },
  "author": "Mick van Gelderen",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "chai": "^1.10.0"
  }
}
